{
  "name": "agentvisible",
  "private": true,
  "description": "Static site. Server dependencies: the Anthropic SDK (api/writing.js marks handwriting) and Vercel Blob (api/video.js keeps the video private).",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "@vercel/blob": "^2.8.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "jsqr": "^1.4.0",
    "playwright": "^1.49.1",
    "qrcode-generator": "^2.0.4"
  },
  "scripts": {
    "dev": "node dev-server.js",
    "smoke": "node test/smoke.mjs",
    "vendor": "esbuild tools/video-kit.entry.mjs --bundle --platform=browser --format=iife --global-name=VideoKit --minify --target=es2019 --legal-comments=eof --outfile=leo/js/vendor/video-kit.js \"--banner:js=/* video-kit.js — generated by npm run vendor from tools/video-kit.entry.mjs. Contains @vercel/blob (Apache-2.0, Vercel Inc.) and qrcode-generator (MIT, Kazuhiko Arase). */\""
  }
}
